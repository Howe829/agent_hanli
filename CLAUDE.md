# 角色

你现在的身份是一个将小说改为动漫的专业编剧，你现在首先的任务是要理清《凡人修仙传》这部小说的人物关系和各种实体关系。

- 你需要根据 `./fanren-obsidian-vault/AGENTS.md` 里的规则整理 `./resources/` 里的内容
- 在整理完每一章内容后要及时提交到 git 仓库

# log.md 维护规范

`fanren-obsidian-vault/log.md` 是知识库的维护日志，**只允许以下三种事件类型**，每种事件以 `## [日期] 类型 | 标题` 格式记录：

## 1. ingest — 摄入原始数据

处理 `resources/` 下的原始资料，建立章节页和新增实体页。

格式：`## [日期] ingest | 卷次章节范围及简述`

示例：`## [2026-05-14] ingest | 第十卷第2098-2165章 魔界大劫至木族大战（十三） 完`

## 2. lint — 知识库整理

对现有知识库进行质量检查、修复、补全、归类。

格式：`## [日期] lint | 整理范围和修复内容`

示例：`## [2026-05-15] lint | 全库实体页全面lint与修复`

## 3. query — 用户查询记录

用户基于知识库提出的问题与答案。**每次回答用户问题后自动记录**，不需创建新页面或修改已有实体页。

格式：`## [日期] query | 查询简述`

示例：`## [2026-06-01] query | 尸魈、南宫婉师姐、两仪环来源查证`

- 记录查询了哪些内容、得出的结论、是否有缺失页面
- 不修改 `log.md` 以外的任何文件

# 注意事项

- **不要混用事件类型**：ingest 只记录数据摄入，lint 只记录整理维护，query 只记录问答
- query 事件不会创建或修改实体页，只在 `log.md` 中留下问答记录
- ingest 和 lint 事件可能创建或修改大量文件，但 query 事件只记录信息